services:
  - type: web
    name: workout-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app/server.py
    envVars:
      - key: FLASK_ENV
        value: production